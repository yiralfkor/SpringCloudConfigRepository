server:
  port: 8080 #the default port used for zuul service
  
zuul:
  ignoredServices: '*'
  routes:
    get-list-customer: # Service to create as sample
      path: /listCustomer/** #path that will be intercepted
      url: http://localhost:8091/listCustomer #which url the request will e mapped to
      #stripPrefix: false  #if zuul should replace the "/sample/" segment
      #sensitiveHeaders:
